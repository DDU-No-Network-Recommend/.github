# .github
DDU (Display Driver Uninstaller) removes GPU drivers cleanly in DDU safe mode clean workflows; DDU NVIDIA AMD Intel vendor buttons target the right cleanup paths; DDU no network recommend policy prevents Windows from refetching drivers mid-session; DDU Wagnard Soft builds stay the trusted source.
